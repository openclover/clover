package org.openclover.eclipse.functest.runner;

import org.eclipse.core.resources.IProject;

import java.io.File;

/** Launches JUnit 4 tests in a subprocess. Implemented in step 6. */
public class TestRunner {

    public static void run(IProject project, String cloverRuntime, File eclipseInstallDir, TestResult result) {
        // TODO step 6: locate JUnit 4 jar, build classpath, spawn JVM subprocess
    }
}
